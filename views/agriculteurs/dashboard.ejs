<!DOCTYPE html>
<html lang="en">
<%- include('head') %>
<body>
    <% if (success_msg.length > 0 || error_msg.length > 0) { %>
        <% if (success_msg.length> 0) { %>
            <div class="absolute w-full h-24 top-0 inset-0 z-50 items-center justify-center flex content-center opacity-100 transition-opacity" id="flash_msg">
                <div class="bg-green-500 text-center text-white flex flex-row p-3 border-5 border-double border-white items-center justify-center m-3 w-72 rounded-lg relative">
                    <% success_msg.forEach(function(msg) { %>
                        <p class="text-white font-bold mr-3"><%= msg %></p>
                        <button type="button" onclick="closeFlash();" class="text-xl text-white text-center items-center justify-center text-left absolute right-0 h-full w-16"><i class="fa-solid fa-circle-xmark"></i></button>
                    <% }) %>
                </div>
            </div>
        <% } else { %>
            <div class="absolute w-full h-24 top-0 inset-0 z-50 items-center justify-center flex content-center" id="flash_msg">
                <div class="bg-red-500 text-center text-white flex flex-row p-3 border-5 border-double border-white items-center justify-center m-3 w-72 rounded-lg relative">
                    <% error_msg.forEach(function(msg) { %>
                        <p class="text-white font-bold mr-3"><%= msg %></p>
                        <button type="button" onclick="closeFlash();" class="text-xl text-white text-center items-center justify-center text-left absolute right-0 h-full w-16"><i class="fa-solid fa-circle-xmark"></i></button>
                    <% }) %>
                </div>
            </div>
        <% } %>
    <% } %>
    <nav class="absolute left-0 top-0 h-screen md:w-32">

    </nav>

    <div class="fixed w-full h-16 top-0 right-0 p-2 md:px-7 shadow-xl flex justify-between items-center bg-white">
        <!-- Logo -->
        <div class="lg:w-28 sm:w-12 h-12 lg:h-24 flex items-center justify-center">
            <img class="h-full w-auto lg:w-44 hidden lg:flex object-contain" src="/images/logo&name.webp" alt="Logo de Fertilia">
            <!-- <img class="h-full w-auto sm:block sm:w-14 lg:hidden object-contain" src="/images/logo_only.webp" alt="Logo de Fertilia"> -->
            <img class="h-full w-auto lg:hidden sm:flex object-contain" src="/images/logo_only.webp" alt="Logo de Fertilia">
        </div>
    
        <!-- Texte + Profil -->
        <div class="flex flex-row items-center ml-auto w-auto h-16">
            <div class="flex flex-row text-center items-center justify-center mr-2">
                <p>Bonjour 👋,</p>
                <h2 class="font-bold ml-1"><%= prenom %></h2>
            </div>
            <div class="w-12 h-12 rounded-full overflow-hidden">
                <img src="/images/images_pofile.jpg" alt="profile" class="object-cover w-full h-full">
            </div>
        </div>
    </div>
    

    <main class="">
        <div class="m-24 p-24">
            <h2 class="text-4xl">👋 hello</h2>
        </div>
        
    </main>
</body>
</html>